<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tienda Estéreo - Pedidos</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <header class="bg-dark text-light py-3">
        <div class="container d-flex justify-content-between align-items-center">
            <h1 class="h3 mb-0">Tienda Estéreo</h1>
            <div>
                <a href="/" class="btn btn-secondary">Inicio</a>
                <a href="/auth/logout" class="btn btn-danger">Cerrar Sesión</a>
            </div>
        </div>
    </header>

    <main class="container my-4">
        <div class="row">
            <div class="col-12 text-center">
                <h1 class="text-secondary">Lista de Pedidos</h1>
            </div>
            <div class="col-12">
                <table class="table table-striped">
                    <thead class="table-dark">
                        <tr>
                            <th scope="col">#</th>
                            <th scope="col">Nombre del Cliente</th>
                            <th scope="col">Valor del Pedido</th>
                            <th scope="col">ID de Productos</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% (orders || []).forEach(order => { %>
                        <tr>
                            <th scope="row"><%= order.id %></th>
                            <td><%= order.customerName %></td>
                            <td>$<%= order.totalPrice.toLocaleString('es-CO') %></td>
                            <td><%= order.productIds.join(', ') %></td>
                        </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
